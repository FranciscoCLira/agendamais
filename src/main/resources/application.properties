# Banco H2 em arquivo (persistente)
# ############################################################

# Configuração do Hibernate DDL - cria e popula o banco do zero
# spring.jpa.hibernate.ddl-auto=create
# app.reload-data=true

# Configuração do Hibernate DDL - mantem o banco sem carga
 spring.jpa.hibernate.ddl-auto=update
 app.reload-data=false
# ----------------------------------------------------------

spring.datasource.username=sa
# Datasource and dialect are profile-specific. Use `application-dev.properties` for H2 and
# `application-prod.properties` for Postgres. Keeping datasource configuration here can
# accidentally cause the wrong dialect to be selected when running with a different profile.
# The dev profile file already contains the H2 configuration and dialect.

# Thymeleaf configuração
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/

# Session timeout - 30 minutes (Spring Boot default)
server.servlet.session.timeout=30m

# Ativa os logs SQL
spring.jpa.show-sql=true

# ----------------------------------------------------------
spring.mail.default-encoding=UTF-8
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Email do gestor
gestor.email=fclira.fcl@gmail.com

# Configurações de email SMTP
spring.mail.username=fclira.fcl@gmail.com
spring.mail.password=saiuwglcfgorfkek
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# ----------------------------------------------------------

# ----------------------------------------------------------
# Email do gestor
# gestor.email=${gestor.email}

# Configurações de email SMTP
# spring.mail.username=${spring.mail.username}
# spring.mail.password=${spring.mail.password}
# spring.mail.host=${spring.mail.host}
# spring.mail.port=${spring.mail.port}
# ----------------------------------------------------------

# spring.mail.username=fclira.fcl@gmail.com
# spring.mail.password=saiuwglcfgorfkek
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# ----------------------------------------------------------

#  Configurar SMTP  - EMAIL HOTMAIL 
#  Pode ser necessario habilitar SMTP AUTH no painel de seguranca da conta
#spring.mail.host=smtp.office365.com
#spring.mail.port=587
#spring.mail.username=fclira.fcl@gmail.com
#spring.mail.password=ycfcxxxxl$2500xx

# Ative logs detalhados do JavaMail
#spring.mail.properties.mail.debug=true



